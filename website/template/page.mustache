<!DOCTYPE html>
<html>
<head>
  <title>Onyxite Customs</title>
  <link rel="stylesheet" type="text/css" href="css/normalize.css">
  <link rel="stylesheet" type="text/css" href="css/styles.css">
</head>
<body>

{{#artists}}
  <div class="onyx-artist">
    <h2>{{ artist }}</h2>
    {{#albums}}
      <div class="onyx-album">
        <h3>{{ album }}</h3>
        {{#songs}}
          <div class="onyx-song">
            <h4>{{ title }}</h4>
            {{#comments}}
              <p>{{{ . }}}</p>
            {{/comments}}
            <ul>
              {{#targets}}
                <li>
                  <code>{{ name }}</code>
                  (<a href="{{ url }}">download</a>)
                </li>
              {{/targets}}
            </ul>
          </div>
        {{/songs}}
      </div>
    {{/albums}}
  </div>
{{/artists}}

</body>
</html>
